<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap 5 Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>

<div class="container-fluid p-2 bg-dark text-white text-start">
  <a href="/" class="btn btn-dark" role="button">Home</a>
  <button class="btn btn-dark dropdown-toggle" type="button" id="accountDropdownButton" data-bs-toggle="dropdown" aria-expanded="false">
    Account
  </button>
  <ul class="dropdown-menu" aria-labelledby="accountDropdownButton">
    <% if user_signed_in? %>     
      <%= button_to "Sign out", destroy_user_session_path, method: :delete %>
    <% else %>
      <li><a class="dropdown-item" href="/users/sign_in">Sign in</a></li>
      <li><a class="dropdown-item" href="/users/sign_up">Sign up</a></li>
    <% end %>
  </ul>
</div>

<div class="container-fluid p-3">
  <h1>Portfolio Project<h1>
  <% if user_signed_in? %>     
    <div> Signed in as: <%= current_user.email %> </div>
  <% end %>
  <hr class="my-4">
</div>

<div class="container-fluid p-3">
  <h2>Projects:</h2>
  <div class="row">
    <% @projects.each do |project| %>
      <span class="border border-dark m-1 p-1">
        <div class="col">
          <h2><%= project.title %></h2>
          <p><%= project.description %></p>
          <br>
          <a class="btn btn-dark m-1" href="/projects/<%= project.id %>">Show this project >></a>
        </div>
      </span>
    <% end %>
  </div>
  <hr class="my-4">
  <% if user_signed_in? %>     
    <a class="btn btn-success btn-lg m-1" href="/projects/new">Add a new project >></a>
  <% end %>
</div>

</body>
</html>